// Store examples of modes of transportation in an array
const transportationModes = ["car", "motorcycle", "bicycle", "train", "plane"];

// Print statements about each mode of transportation
for (let i = 0; i < transportationModes.length; i++) {
    const mode = transportationModes[i];
    console.log(`I would like to own a ${mode}.`);
}
